# Split homology

Snakemake pipeline implementation for split homolog candidate identification.

Usage: `./run all`

Note: default behavior is to submit to a SLURM queue. You can change this behavior by removing/changing the `--slurm --profile smk_profile` flags in the `run` script.
